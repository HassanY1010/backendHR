services:
  - type: web
    name: hr-backend
    env: node
    plan: starter
    buildCommand: npm ci && npx prisma generate
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
